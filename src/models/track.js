export const track = {
  _id: string,
  title: string,
  artist: string,
  album: string,
  s3Url: string,
  duration: number, // En segundos
  price: number, // Precio en dólares
  createdAt: date,
}
